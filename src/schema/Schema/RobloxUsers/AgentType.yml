name: AgentType

database: RobloxUsers
table: AgentTypes

namespace: Roblox.Platform.Agents.Entities

version: 2

predefined:
  method: GetOrCreate
  
  values:
    - name: User
      properties:
        - Value: "User"
    - name: Group
      properties:
        - Value: "Group"
      
id:
  type: byte

properties:
  - name: Value
    description: Gets the name of the agent type
    type: string
    nullable: false
    read_only: false
  - name: Created
    type: DateTime
    nullable: false
    read_only: false
  - name: Updated
    type: DateTime
    nullable: true
    read_only: false

methods:
  - name: GetByValue
    type: Lookup
    parameters:
      - name: value
        type: string
  
  - name: GetOrCreate
    type: GetOrCreate
    parameters:
      - name: value
        type: string
